--- a/isaaclab.sh
+++ b/isaaclab.sh
@@ -17,5 +17,5 @@
 
 # get source directory
-export ISAACLAB_PATH="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
+export ISAACLAB_PATH="$( cd "$( dirname $(realpath ${BASH_SOURCE}) )" &> /dev/null && pwd )"
 
 #==
